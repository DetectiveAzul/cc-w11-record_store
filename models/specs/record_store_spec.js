const assert = require('assert');
const RecordCollector = require('../record_store.js');

describe('Store', function () {
  let store;

  beforeEach(function() {
    store = new Store();
  });

  it('should have a name');
  it('should start with no funds');
  it('should be able to add funds');
  it('should start with an empty collection of records');
  it('should be able to add a record to its stock');
  it('should be able to sell a record if it has the record');

});
